from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Assessing the different options for increasing temporal granularity in elecsim',
    author='Alexander Kell',
    license='MIT',
)
